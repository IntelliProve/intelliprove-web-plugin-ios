# intelliprove-web-plugin-ios

# Installation
## Prerequisites
- You have Xcode updated to 26.1

# Build SDK
- Build the XCFramework file by running `source ./build-xcframework.sh`.
- Find the XCFramework folder under _/output/IntelliProveSDK.xcframework_.

